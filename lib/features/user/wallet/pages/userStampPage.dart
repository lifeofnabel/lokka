import 'package:flutter/material.dart';

class UserStampPage extends StatelessWidget {
  const UserStampPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('user Stamp Page'),
      ),
    );
  }
}

