import 'package:flutter/material.dart';

class CampaignClaimPage extends StatelessWidget {
  const CampaignClaimPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('campaign Claim Page'),
      ),
    );
  }
}

