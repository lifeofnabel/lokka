import 'package:flutter/material.dart';

class ClaimSuccessPage extends StatelessWidget {
  const ClaimSuccessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('claim Success Page'),
      ),
    );
  }
}

