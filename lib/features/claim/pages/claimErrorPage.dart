import 'package:flutter/material.dart';

class ClaimErrorPage extends StatelessWidget {
  const ClaimErrorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('claim Error Page'),
      ),
    );
  }
}

