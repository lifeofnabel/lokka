import 'package:flutter/material.dart';

class AuthRoleGatePage extends StatelessWidget {
  const AuthRoleGatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('auth Role Gate Page'),
      ),
    );
  }
}

