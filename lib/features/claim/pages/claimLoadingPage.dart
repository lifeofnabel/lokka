import 'package:flutter/material.dart';

class ClaimLoadingPage extends StatelessWidget {
  const ClaimLoadingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('claim Loading Page'),
      ),
    );
  }
}

