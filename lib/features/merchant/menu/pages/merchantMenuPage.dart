import 'package:flutter/material.dart';

class MerchantMenuPage extends StatelessWidget {
  const MerchantMenuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('merchant Menu Page'),
      ),
    );
  }
}

