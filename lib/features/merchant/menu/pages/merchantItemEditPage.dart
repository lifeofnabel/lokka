import 'package:flutter/material.dart';

class MerchantItemEditPage extends StatelessWidget {
  const MerchantItemEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('merchant Item Edit Page'),
      ),
    );
  }
}

