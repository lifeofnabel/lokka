import 'package:flutter/material.dart';

class StampClaimPage extends StatelessWidget {
  const StampClaimPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('stamp Claim Page'),
      ),
    );
  }
}

