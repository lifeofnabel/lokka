import 'package:flutter/material.dart';

class UserPartnerDetailPage extends StatelessWidget {
  const UserPartnerDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('user Partner Detail Page'),
      ),
    );
  }
}

