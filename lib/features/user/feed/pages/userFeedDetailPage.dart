import 'package:flutter/material.dart';

class UserFeedDetailPage extends StatelessWidget {
  const UserFeedDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('user Feed Detail Page'),
      ),
    );
  }
}

