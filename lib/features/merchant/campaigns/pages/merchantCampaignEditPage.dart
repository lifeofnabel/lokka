import 'package:flutter/material.dart';

class MerchantCampaignEditPage extends StatelessWidget {
  const MerchantCampaignEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('merchant Campaign Edit Page'),
      ),
    );
  }
}

