import 'package:flutter/material.dart';

class UserPartnersPage extends StatelessWidget {
  const UserPartnersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('user Partners Page'),
      ),
    );
  }
}

